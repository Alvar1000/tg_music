# Бот канала @blackmagicwoman 🤘

Telegram-бот для рок-сообщества. Конвертирует тех, кто зашёл в бота, в подписчиков
канала (через гейт подписки) и развлекает контентом: плейлист дня, факты о роке,
тесты и квест.

## Возможности

- **Гейт подписки.** Пока пользователь не подписан на канал — функционал закрыт.
- **🎵 Плейлист дня** — очередь подборок: по одной в день, пополняется загрузкой файла через бота (см. «Плейлисты: очередь и поведение на сервере»).
- **🎲 Рандомный факт** — факты без повторов для каждого пользователя.
- **🧠 Тесты:**
  - *Музыкант по знаку зодиака* — 12 знаков → музыкант с описанием.
  - *Угадай группу по обложке (Часть 1, 2 и 3)* — тесты по обложкам: фото альбома → варианты групп, разбор каждого ответа и итоговый счёт (на FSM).
  - *Кто ты из рок/метал-музыкантов?* — 10 вопросов с вариантами ответа, каждый вариант «голосует» за музыканта; в конце показывается тот, за кого набралось больше всего очков (на FSM).
  - *Квест «Спаси концерт»* — ветвящийся сюжет (универсальный движок графа).
  - *🧩 Найди группу* — Telegram Mini App (словопоиск): одна и та же «афиша дня» с
    именами рок-групп у всех пользователей, результат (время) сохраняется. См. «Мини-игра
    «Найди группу» (Telegram Mini App)» ниже.
- **📅 Мероприятия** — список из `content/events.json`.
- **💬 Общение** — ссылка на чат сообщества.
- **/stats** — статистика для админов за сегодня (UTC): заходили, подписаны, тесты,
  открытия «Плейлиста дня», заходы/прохождения «Найди группу».
- **/month** — та же статистика суммой за последние 30 дней, плюс MAU и средний DAU.
- **/broadcast** — рассылка сообщения (текст/фото/видео) всем пользователям бота, с
  превью и подтверждением перед отправкой.

## Технологии

- Python 3.11+
- [aiogram 3.x](https://docs.aiogram.dev/) (async, роутеры, встроенный FSM)
- aiosqlite + SQLite (локальная база, без внешних сервисов)
- python-dotenv (секреты в `.env`)
- aiohttp (веб-сервер мини-игры, в одном процессе с ботом — см. ниже)
- Режим: **long polling** (см. раздел про webhook ниже)

## Структура

```
main.py                  # запуск: логирование, бот, диспетчер, polling + веб-сервер мини-игры
config.py                # чтение .env + загрузка контента из JSON
server.py                # aiohttp: страница и API мини-игры «Найди группу»
webapp/rockle/index.html # сама мини-игра (статика, самодостаточный HTML/CSS/JS)
requirements.txt
.env.example
handlers/
  start.py               # /start + гейт подписки
  menu.py                # главное меню, плейлист, чат, UI-помощники
  facts.py               # рандомные факты без повторов
  tests.py               # выбор теста + движки зодиака, обложек, музыкантов и квеста
  events.py              # мероприятия
  admin.py               # /stats
keyboards/kb.py          # все inline-клавиатуры
middlewares/subscription.py  # проверка подписки на каждое действие
database/db.py           # инициализация и запросы к SQLite
states/states.py         # FSM-состояния для тестов по обложкам/музыкантам и квеста
content/                 # весь контент в JSON (правится без программиста)
  facts.json
  quiz_zodiac.json
  quiz_covers_1.json     # тест по обложкам, Часть 1
  quiz_covers_2.json     # тест по обложкам, Часть 2
  quiz_covers_3.json     # тест по обложкам, Часть 3
  quiz_musician.json     # тест «Кто ты из рок/метал-музыкантов?»
  covers/                # файлы обложек альбомов (фото к вопросам)
  rockle_words.json      # пул групп для мини-игры «Найди группу»
  quest_concert.json     # нужно добавить (см. ниже)
  events.json            # нужно добавить (см. ниже)
```

**Принцип:** вся логика — в коде, весь контент — в `content/*.json` и `.env`.
Чтобы добавить факты, поменять плейлист или отредактировать тест, Python трогать не нужно.

## Подготовка перед запуском

1. **Создай бота** у [@BotFather](https://t.me/BotFather) → получи токен.
2. **Сделай бота администратором канала** @blackmagicwoman.
   Это обязательно: без прав админа метод `get_chat_member` не сможет проверять
   подписку, и гейт будет закрыт для всех. Достаточно минимальных прав
   (главное — чтобы бот видел участников).
3. **Узнай свой Telegram id** у [@userinfobot](https://t.me/userinfobot) — для `/stats`.
4. Приготовь ссылки: на плейлист в Яндекс.Музыке и на чат сообщества.

## Установка и запуск

```bash
# 1. Виртуальное окружение (если ещё нет)
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate

# 2. Зависимости
pip install -r requirements.txt

# 3. Настройки
cp .env.example .env
# открой .env и заполни BOT_TOKEN, CHANNEL_ID, ссылки, ADMIN_IDS

# 4. Запуск
python main.py
```

Бот запускается командой `python main.py` без правок кода — нужно только заполнить `.env`.
База `bot.db` и лог `bot.log` создаются автоматически рядом с `main.py`.

## Переменные `.env`

| Переменная | Назначение |
|---|---|
| `BOT_TOKEN` | токен от @BotFather (обязательно) |
| `CHANNEL_ID` | `@username` или числовой `-100...` (обязательно) |
| `CHANNEL_URL` | ссылка на канал (кнопка «Подписаться») |
| `COMMUNITY_CHAT_URL` | ссылка на чат сообщества |
| `YANDEX_PLAYLIST_URL` | ссылка на плейлист дня |
| `ADMIN_IDS` | id админов через запятую |
| `RESET_FACTS_WHEN_DONE` | `true` — крутить факты по кругу; `false` — показать «закончились» |
| `WEBAPP_URL` | (необязательно) публичный HTTPS-адрес мини-игры «Найди группу». На Render не нужен — подхватывается сам из `RENDER_EXTERNAL_URL` |
| `PORT` | (необязательно) порт aiohttp-сервера мини-игры, по умолчанию `8080`. На Render подставляется сам |

## Редактирование контента

Все файлы в `content/` — обычный JSON в UTF-8. После правки перезапуск не нужен:
бот читает файлы заново при каждом запросе.

- **Картинка меню** — положи файл `content/menu.jpg` (или `.png`), и он покажется баннером
  над главным меню. Можно задать другой путь или URL через `MENU_IMAGE` в `.env`.
  Нет файла — меню просто текстовое.
- **`facts.json`** — список фактов: `[{"id": 1, "text": "..."}, ...]`. У каждого факта
  уникальный `id` (по нему бот помнит, что пользователь уже видел).
- **`quiz_zodiac.json`** — `{"Овен": {"name": "...", "desc": "..."}, ...}` (все 12 знаков).
- **`quiz_covers_1.json` / `quiz_covers_2.json`** — два теста «Угадай группу по обложке».
  Формат: `{"title": "...", "questions": [...]}`, где каждый вопрос —
  ```json
  {
    "photo": "nirvana-in-utero.jpg",   // файл в content/covers/
    "group": "Nirvana",                 // правильная группа (для разбора ответа)
    "album": "In Utero",               // название альбома (опционально, для разбора)
    "options": ["Pearl Jam", "Soundgarden", "Nirvana", "Alice in Chains"],
    "correct": 2                        // индекс правильного варианта в options
  }
  ```
  Картинки лежат в `content/covers/`. Число вариантов берётся из `options` (можно 3, можно 4).
  Чтобы добавить вопрос — положи обложку в `content/covers/` и допиши объект в массив.
- **`quiz_musician.json`** — тест «Кто ты из рок/метал-музыкантов?». Формат:
  ```json
  {
    "title": "...",
    "questions": [
      {
        "text": "Текст вопроса",
        "options": [
          {"text": "Вариант ответа", "result": "cobain"},
          {"text": "Другой вариант", "result": "axl"}
        ]
      }
    ],
    "results": {
      "cobain": {"emoji": "🌫️", "name": "Курт Кобейн (Nirvana)", "desc": "..."},
      "axl": {"emoji": "🎤", "name": "Эксл Роуз (Guns N' Roses)", "desc": "..."}
    }
  }
  ```
  Каждый вариант ответа «голосует» за ключ в `result` (должен быть в `results`). После
  последнего вопроса побеждает музыкант с наибольшим числом голосов; ничьи решает монетка
  (`random.choice`). Число вопросов и вариантов не фиксировано — можно добавлять и то, и
  другое, не трогая код.
- **`quest_concert.json`** — граф квеста. Узлы (`nodes`) бывают трёх видов:
  ```json
  {
    "start": "S0",
    "nodes": {
      "S0": {
        "text": "Текст ситуации.\n\nЧто делаешь?",
        "choices": [
          {"text": "Вариант А", "next": "S_NEXT"},
          {"text": "Вариант Б", "next": "ENDING_BAD"}
        ]
      },
      "S_NEXT": {
        "text": "Промежуточная сцена без выбора.",
        "next": "ENDING_GOOD"
      },
      "ENDING_GOOD": {
        "ending": true,
        "title": "Название концовки",
        "text": "Финальный текст.",
        "verdict": "Что в итоге.",
        "rank": "Звание игрока",
        "rarity": "Очень редкая",
        "score": "10/10"
      },
      "ENDING_BAD": { "ending": true, "title": "Провал", "text": "...", "rarity": "Обычная", "score": "3/10" }
    }
  }
  ```
  - **Сюжетный узел** — `text` + `choices` (кнопки ведут на `next`).
  - **Нода-проходка** — `text` + одно поле `next` (без `choices`): бот покажет одну кнопку «Дальше →».
  - **Концовка** — `"ending": true`. Поля `title`, `verdict`, `rank`, `rarity`, `score` необязательны
    (чего нет — просто не покажется). Бот считает открытые концовки и пишет «Открыто: N/всего».
  - Текст пишется **обычным текстом, без HTML** (бот сам экранирует и оформляет). Id узлов держи
    короткими — они уходят в `callback_data`, лимит Telegram 64 байта.
- **`events.json`** — список мероприятий (нужно создать). Поле `desc` необязательное —
  свободный текст (программа вечера, цена, возрастное ограничение и т.п.), выводится
  отдельным абзацем под `title`/`date`/`place`:
  ```json
  [
    {"title": "Квартирник", "date": "Пятница, 20:00", "place": "Бар «Чёрная кошка»", "desc": "", "url": ""}
  ]
  ```

> Пока `events.json` нет — раздел «Мероприятия» показывает аккуратную заглушку и бот не падает.
> Как добавишь файл — заработает само.

## Плейлисты: очередь и поведение на сервере

«Плейлист дня» — это **очередь** из `content/playlists.json` (массив объектов
`{"title", "desc", "url"}`). Бот показывает по одной подборке в день, сдвигая
указатель на следующую каждый календарный день (UTC) и останавливаясь на последней,
пока не добавишь новые. Указатель хранится в БД (таблица `playlist_state`).

Пополнять очередь можно:
- **через бота** — отправь админ-аккаунтом `.json`-файл (тот же формат: непустой
  массив объектов, у каждого `url` на `http(s)`). Новые дописываются в конец, дубли
  по `url` отсекаются. Команда `/playlists` показывает статус очереди;
- **правкой `content/playlists.json`** с последующим перезапуском/редеплоем.

### ⚠️ Особенность на хостинге с постоянным диском (Render и т.п.)

Файловая система контейнера эфемерная, поэтому состояние выносят на диск
переменными окружения: база — `DB_PATH=/data/bot.db`, очередь —
`PLAYLISTS_PATH=/data/playlists.json`. Из-за этого с плейлистами есть нюанс:

- `content/playlists.json` в репозитории — лишь **стартовое «зерно»**. Оно
  копируется на диск **один раз** при первом запуске, если `/data/playlists.json`
  ещё нет (функция `seed_playlists` в `config.py`).
- После первого запуска **живая очередь живёт на диске**, не в git. Правки
  `content/playlists.json` + `git push` её **не меняют** — файл на диске «затеняет»
  репозиторный.
- Значит, на сервере очередь меняй **загрузкой `.json` через бота** (пишет прямо на
  диск, переживает деплои). Либо вручную: отредактируй `/data/playlists.json` во
  вкладке Shell, либо удали его и сделай редеплой — пересеется из «зерна».
- Резервная копия БД — команда `/backup` (бот пришлёт `bot.db` файлом в чат админа).

Хочешь вести очередь **только через git** — не задавай `PLAYLISTS_PATH`: тогда
источник правды снова `content/playlists.json`, но загрузка через бота на сервере
перестанет сохраняться (файл опять станет эфемерным).

## Мини-игра «Найди группу» (Telegram Mini App)

`🧩 Найди группу` в разделе «Тесты» — словопоиск (сетка букв, ищешь спрятанные
названия рок-групп), открывается как настоящий [Telegram Mini
App](https://core.telegram.org/bots/webapps) прямо в чате, без перехода на
внешний сайт. Устроено так:

- **Один процесс, два дела.** `main.py` поднимает aiohttp-сервер (`server.py`) и
  запускает long polling бота **в одном и том же процессе** — не отдельным
  сервисом. Причина: постоянный диск Render (`/data`, там `bot.db` и
  `playlists.json`) можно примонтировать только к одному сервису, а второй
  процесс до тех же файлов просто не достучится.
- **Афиша дня общая для всех.** `GET /api/rockle/today` каждый день детерминированно
  выбирает 10 групп из `content/rockle_words.json` (`random.Random(today).sample(...)`
  — тот же принцип, что у «Плейлиста дня»). Сама сетка букв строится **на клиенте**,
  тоже детерминированно (сид — дата), поэтому у всех пользователей в этот день
  она пиксель-в-пиксель одинаковая — честно сравнивать время прохождения.
- **Результат защищён от подделки.** Мини-апка получает от Telegram подписанный
  `initData` и присылает его вместе с результатом на `POST /api/rockle/complete`.
  Сервер проверяет HMAC-подпись (`server.py:validate_init_data`, алгоритм из
  [документации Telegram](https://core.telegram.org/bots/webapps#validating-data-received-via-the-mini-app))
  и только после этого достаёт `user_id` — без проверки любой мог бы прислать
  чужой id. Первое прохождение за день сохраняется в `rockle_results`, повторные
  попытки его не перезаписывают.
- **Кнопка условная.** `keyboards/kb.py:tests_menu_kb()` показывает кнопку, только
  если `config.WEBAPP_URL` заполнен (Telegram Mini App обязательно требует
  HTTPS — локально без туннеля вроде ngrok кнопки не будет).
- Пул групп — обычный контент, `content/rockle_words.json` (формат: массив
  `{"display", "key"}`, `key` — заглавные буквы без пробелов/пунктуации, по нему
  ищутся слова в сетке). Пополняется как любой другой файл в `content/`.

## Переключение на webhook

По умолчанию бот работает на long polling — это проще: процесс сам опрашивает Telegram,
внешний адрес не нужен. Для продакшена иногда удобнее webhook (Telegram сам шлёт апдейты
на твой HTTPS-адрес). Нужен публичный домен с валидным SSL.

> aiohttp-сервер уже поднят в этом же процессе ради мини-игры (`server.py`) — при
> переходе на webhook регистрируй `SimpleRequestHandler` на **том же** объекте
> `web.Application`, что создаёт `server.py:create_app()`, а не поднимай второй.

Замени в `main.py` запуск polling на webhook-сервер (aiohttp идёт в комплекте с aiogram):

```python
from aiohttp import web
from aiogram.webhook.aiohttp_server import SimpleRequestHandler, setup_application

WEBHOOK_PATH = "/webhook"
WEBHOOK_URL = "https://ТВОЙ_ДОМЕН" + WEBHOOK_PATH

async def main() -> None:
    setup_logging()
    config.validate()
    await db.init_db()
    bot = create_bot()
    dp = create_dispatcher()
    await bot.set_webhook(WEBHOOK_URL)

    app = web.Application()
    SimpleRequestHandler(dispatcher=dp, bot=bot).register(app, path=WEBHOOK_PATH)
    setup_application(app, dp, bot=bot)
    web.run_app(app, host="0.0.0.0", port=8080)
```

При long polling, наоборот, убедись, что webhook снят: `await bot.delete_webhook()`.

## Деплой на Render (Blueprint, «одной кнопкой»)

В репозитории есть [`render.yaml`](render.yaml) — он описывает весь сервис разом:
веб-сервис (`type: web` — нужен публичный порт для мини-игры, не `worker`), постоянный
диск `/data` (для `bot.db` и очереди плейлистов) и переменные окружения. Благодаря
ему деплой с нуля не нужно собирать руками в дашборде.

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/Alvar1000/tg_music)

Или вручную: **Render Dashboard → New → Blueprint → выбрать этот репозиторий**.
Render прочитает `render.yaml`, создаст веб-сервис + диск + переменные и **один раз
спросит значения секретов** (`BOT_TOKEN`, `CHANNEL_ID`, `CHANNEL_URL`, `COMMUNITY_CHAT_URL`,
`ADMIN_IDS` — они помечены `sync: false` и в git не хранятся). Пути `DB_PATH` и
`PLAYLISTS_PATH` уже прописаны в файле; `WEBAPP_URL` для кнопки мини-игры Render
заполняет сам через `RENDER_EXTERNAL_URL` (см. «Мини-игра «Найди группу»» выше).

После первого запуска бот сам создаёт таблицы в `/data/bot.db` и засевает 25 стартовых
плейлистов из `content/playlists.json` на диск. Дальше плейлисты добавляются загрузкой
`.json` через бота (см. «Плейлисты: очередь и поведение на сервере»).

> **Один экземпляр на токен.** Telegram разрешает только один `getUpdates`, поэтому
> перед деплоем Blueprint удали старый сервис на том же `BOT_TOKEN` — иначе два бота
> подерутся за апдейты (ошибка `409 Conflict`).

## Хостинг

Для long polling подойдёт любой дешёвый VPS или платформа вроде Railway/Render.
Главное — процесс `python main.py` должен работать постоянно (используй systemd,
`screen`/`tmux` или менеджер процессов платформы).

## Модель данных (SQLite)

- `users(user_id, username, full_name, first_seen, last_active, is_subscribed)`
- `seen_facts(user_id, fact_id, seen_at)` — какие факты кто видел
- `quiz_results(user_id, quiz_name, result, completed_at)` — результаты тестов/квеста
- `seen_endings(user_id, ending_id, seen_at)` — открытые концовки квеста
- `playlist_state(id, current_index, last_advance)` — указатель «Плейлиста дня» (одна строка)
- `feature_usage(user_id, feature, used_at)` — лог разовых действий (для `/stats`/`/month`);
  `feature = 'playlist'` — открытие «Плейлиста дня», `feature = 'rockle_open'` — заход в
  мини-игру «Найди группу» (учитывается при каждой загрузке страницы, а не только при
  завершении — см. `rockle_results` ниже)
- `daily_active(user_id, day)` — по одной строке на пользователя за день, пишется при
  любом действии в боте; источник MAU и среднего DAU для `/month`
  (`PRIMARY KEY (user_id, day)`)
- `rockle_results(user_id, play_date, seconds, completed_at)` — результаты мини-игры «Найди группу», по одному на пользователя в день (`PRIMARY KEY (user_id, play_date)`)
